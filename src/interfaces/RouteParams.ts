export type QuizPageParams = {
  levelId: string;
  classId: string;
  topicId: string;
  quizType: string;
};

export type LevelPageParams = {
  levelId: string;
};

export type ReportPageParams = {
  levelId: string;
  classId: string;
};

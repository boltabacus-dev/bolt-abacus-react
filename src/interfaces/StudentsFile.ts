export type Student = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
};

export type StudentsFileRowType = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
};

export type TeacherStudent = {
  userId: number;
  firstName: string;
  lastName: string;
};

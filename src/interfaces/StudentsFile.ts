export type Student = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
};

export type StudentsFileRowType = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
};
